const swiper = new Swiper('.swiper', {
    // Optional parameters
    direction: 'horizontal',
    loop: false,
    width:220,
    infinite: true,
    slidesPerView:'auto',
    spaceBetween:16,
    followFinger:true,
    freeMode:true,
    animating:true,
  });